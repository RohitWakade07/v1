[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --workers 4 --bind 0.0.0.0:$PORT --timeout 30"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
