# Week 7 Web Scraper
This is the week 7 web scraper assignment. It scrapes data and saves it into the corpus directory as JSON files. It includes a retry mechanism, uses time.sleep to avoid rate limits, and uses BeautifulSoup for parsing the HTML DOM structure.
