# API base URL
# In local dev, Vite proxies /api → localhost:8000 so this is typically not needed.
# In production (Vercel/Netlify), set this to your Railway API URL:
# VITE_API_BASE_URL=https://your-api.railway.app/api/v1
VITE_API_BASE_URL=
